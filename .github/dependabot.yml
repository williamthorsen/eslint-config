# yaml-language-server: $schema=https://json.schemastore.org/dependabot-2.0.json
version: 2

updates:
  # Bump the `uses:` refs in .github/workflows, grouped into one pull request a month so that each run needs one review.
  - package-ecosystem: github-actions
    directory: /
    # Naming labels explicitly stops dependabot applying its defaults.
    # Any labels listed here and not declared in .github/labels.yaml would be deleted by the next sync.
    labels:
      - ci
      - dependencies
    schedule:
      interval: monthly
    groups:
      actions:
        patterns:
          - '*'
