[
  {
    "version": "11.0.0",
    "date": "2026-08-12",
    "sections": [
      {
        "title": "🎉 Features",
        "audience": "all",
        "items": [
          {
            "description": "Add an init command that scaffolds a config file (#163)",
            "body": "Adds `strict-lint init`, which scaffolds a `.config/strict-lint.config.ts` in the working directory. `--dry-run` reports without writing, `--force` replaces a diverged config, and a config already matching the template is left untouched and reported as up to date. A locally installed package is not required; the command can be run via `npx @williamthorsen/strict-lint init`.\n\nSeparately, the help flag (`-h` or `--help`) is now usable both with the bare `strict-lint` command and its `strict-lint init` subcommand.",
            "hash": "f6df4bb66b46579c2788ca8cc90a38383bf35439"
          },
          {
            "description": "Enforce package-json/specify-peers-locally (#165)",
            "body": "Enforces `package-json/specify-peers-locally`. Every peer dependency a package declares must also appear in its own `devDependencies`; in a pnpm workspace a catalog entry satisfies it without repeating the version.\n\nMigration: A consumer relying on the workspace root's hoisting or on pnpm's `autoInstallPeers` to resolve its peers gets new errors until each peer is declared locally.",
            "breaking": true,
            "hash": "5d8423112c49df8da8ffcd60b5a04ee840f9d637"
          }
        ]
      }
    ]
  },
  {
    "version": "10.0.0",
    "date": "2026-08-11",
    "sections": [
      {
        "title": "🎉 Features",
        "audience": "all",
        "items": [
          {
            "description": "Require author, engines, and publish metadata in package.json (#162)",
            "body": "Adds six `package-json` rules to `@williamthorsen/eslint-config-typescript` and removes three overrides that suppressed rules the plugin's `recommended` preset supplies. Every `package.json` must now declare `author` and `engines` and carry no empty field; one that publishes must also declare `bugs`, `homepage`, `keywords`, `repository`, and `sideEffects`, and may not take a dependency on a local path. `specify-peers-locally` is the only rule the config still disables.",
            "breaking": true,
            "hash": "4511d4162cd04ae78a2f9f985cc1f7c2c3e650b8"
          }
        ]
      },
      {
        "title": "📦 Dependencies",
        "audience": "dev",
        "items": [
          {
            "description": "Upgrade to toolbelt.filesystem v0.3.0 (#156)",
            "body": "Upgrades `@williamthorsen/toolbelt.filesystem` to 0.3.0 and adds `@williamthorsen/toolbelt.packaging` 0.2.0 as a second runtime dependency.",
            "hash": "05e8b8c797e60a93f82573edc19254eb2bb0252e"
          }
        ]
      },
      {
        "title": "🤖 Agentic support",
        "audience": "dev",
        "items": [
          {
            "description": "Update AGENTS.md (#161)",
            "body": "Updates project guidance in AGENTS.md. In particular, most information about the `nmr` task runner has been removed in deference to the guidance bundled with `nmr` itself. `nmr check` is now promoted as the usual check in place of the much slower, and usually unnecessary, `nmr ci`.",
            "hash": "fb93700ab1158102c9aac1ce1b432e8aa1a67745"
          }
        ]
      }
    ]
  },
  {
    "version": "9.4.0",
    "date": "2026-08-07",
    "sections": [
      {
        "title": "🎉 Features",
        "audience": "all",
        "items": [
          {
            "description": "Support explicit resource management for consumers (#148)",
            "body": "Adds `ESNext.Disposable` to the libraries recognized by TypeScript in the exported base config. This enables the use of `using` and `await using` declarations.",
            "hash": "df0113e43e8ee9994c7d20e5e56fc577f9ca67b3"
          }
        ]
      }
    ]
  },
  {
    "version": "9.3.0",
    "date": "2026-08-04",
    "sections": [
      {
        "title": "🎉 Features",
        "audience": "all",
        "items": [
          {
            "description": "Resolve config per linted file and support concurrency (#141)",
            "body": "`strict-lint` now reports what `eslint` reports for every package in a monorepo. It is no longer necessary to have a separate run per workspace for correct results. A package's own `.config/strict-lint.config.ts` now sets the severity ceilings for that package's files, whichever directory the run starts from.\n\n`--concurrency` now works and is off by default, as it is in `eslint`. It is unavailable when an ESLint config is supplied programmatically.\n\nLinting with a TypeScript ESLint config now requires `jiti`. Such a config may now use any TypeScript syntax, not only the subset that can be stripped to plain JavaScript.",
            "hash": "fdef5ad09cecc702808b6711a6a8543eae5a2d29"
          }
        ]
      },
      {
        "title": "🐛 Bug fixes",
        "audience": "all",
        "items": [
          {
            "description": "Stop printing a blank line when nothing is reported (#140)",
            "body": "Fixes an issue where a `strict-lint` run that found no problems still printed a blank line, potentially resulting in many blank lines when run recursively in a monorepo. Separately, when a run exceeds its `--max-warnings` threshold with nothing else to report, the message saying so now starts at the top of the output instead of below a blank line.",
            "hash": "ee18b45a9672217a6916a9a82fafb5c454563079"
          }
        ]
      },
      {
        "title": "⚙️ Tooling",
        "audience": "dev",
        "items": [
          {
            "description": "Migrate the test suite to nmr's isolation tiers (#137)",
            "body": "Changes Vitest configuration so that test suites are selected by a tier (\"unit\", \"tool\", \"localhost\", and \"remote\") corresponding to the services they use. `nmr test:unit` and `nmr test:tool` each run one of these; `nmr test:all` runs every suite. A test verifies that all test file names include the tier infix. `nmr test:integration` no longer exists, and no tests carry the `.int.` infix.",
            "hash": "c2ce64bf7eecc1ced1c7c128288b0c0bf91d3544"
          }
        ]
      }
    ]
  },
  {
    "version": "9.2.0",
    "date": "2026-08-02",
    "sections": [
      {
        "title": "🎉 Features",
        "audience": "all",
        "items": [
          {
            "description": "Add defineConfig for authoring strict-lint configs (#133)",
            "body": "Adds a `defineConfig` helper function for authoring the `strict-lint` config file.",
            "hash": "c7b26ff68ce6daaff9d2ea5bdfc1040d757e78d6"
          }
        ]
      },
      {
        "title": "🐛 Bug fixes",
        "audience": "all",
        "items": [
          {
            "description": "Correct misbehaving Vitest rules (#132)",
            "body": "Corrects some Vitest rules that were triggering broken behavior in repos using them. The Vitest config is now used in this repo's own linting configuration; some violations surfaced by the newly enabled rules have been fixed. The ESLint config for TypeScript now exports `patterns.testFiles`, an array of file patterns commonly used for tests.",
            "hash": "7e19def4b5e2229efccb2c3bd75a243c0d4e3552"
          }
        ]
      }
    ]
  },
  {
    "version": "9.1.0",
    "date": "2026-08-01",
    "sections": [
      {
        "title": "🎉 Features",
        "audience": "all",
        "items": [
          {
            "description": "Accept every ESLint severity as a maxSeverity ceiling (#127)",
            "body": "The max-severity feature of `strict-lint` now accepts every ESLint severity, instead of only `'warn'` and `'error'`. A rule's severity can also be set to `undefined`, which unsets a value inherited for that rule.",
            "hash": "ffaa4e062160ced13bf9453dff202c7ad06432f3"
          }
        ]
      },
      {
        "title": "⚙️ Tooling",
        "audience": "dev",
        "items": [
          {
            "description": "Migrate Vitest configs to the nmr projects model (#126)",
            "body": "The suite in which a test runs is now determined by filename alone: An `.int.` infix marks an integration test; an `.app.` marks an app test; and anything else under `__tests__` runs as a unit test. Integration tests are again excluded from the default test run and from the standard check, and are instead run by `nmr test:integration`. A new workspace picks up the same rules without a test config.",
            "hash": "d6f8e243f262c2a7524715ecfe008422fcbfb1a3"
          }
        ]
      }
    ]
  },
  {
    "version": "9.0.1",
    "date": "2026-07-26",
    "sections": [
      {
        "title": "⚙️ Tooling",
        "audience": "dev",
        "items": [
          {
            "description": "Consume TypeScript config from source (#121)",
            "body": "Simplifies the repo's own ESLint configuration and migrates the configuration to TypeScript. Tooling no longer depends on a build step; `nmr build` now serves publishing alone. The `strict-lint` command runs its compiled entry point directly instead of a committed launcher. `tsx` is no longer a dependency.",
            "hash": "544838248f83ff41565fbd35ed45ddbdb68cc6d2"
          }
        ]
      }
    ]
  },
  {
    "version": "9.0.0",
    "date": "2026-07-24",
    "sections": [
      {
        "title": "🎉 Features",
        "audience": "all",
        "items": [
          {
            "description": "Bound config discovery and remove the built-in allowlist (#115)",
            "body": "`strict-lint` now stops at the project root when walking up the directory tree to find configs. Within the project, all configs on the path from the project root to the directory where the command is run are merged; later settings prevail. A new `--debug` flag reports the resolved project root and the config files that contributed.\r\n\r\n`@williamthorsen/strict-lint` no longer carries built-in exemptions: The style and modernization rules that used to stay warnings now fail the lint run unless a config exempts them. Projects that want the previous exemptions can now import them from `@williamthorsen/eslint-config-typescript`, which exports that set of downgrades as `advisoryRuleSeverities`.",
            "breaking": true,
            "hash": "c54ee1ffb28d974a551e32f8b03387a08101050e"
          }
        ]
      },
      {
        "title": "🐛 Bug fixes",
        "audience": "all",
        "items": [
          {
            "description": "Apply the nearest strict-lint config found from the working directory (#112)",
            "body": "Fixes the issue that `strict-lint`, if run from a workspace, would ignore the workspace-level `strict-lint` configuration if no ESLint configuration file existed at the same level. `strict-lint` now finds its configuration by searching upward from the directory the run starts in, rather than from the nearest ESLint config. The nearest configuration is used outright; configurations at farther levels are ignored rather than merged into it. The search ascends to the filesystem root.",
            "hash": "97267d36cd868d04e5136197341fa99402ca7e25"
          }
        ]
      }
    ]
  },
  {
    "version": "8.0.0",
    "date": "2026-07-20",
    "sections": [
      {
        "title": "🎉 Features",
        "audience": "all",
        "items": [
          {
            "description": "Support .ts ESLint config file types (#104)",
            "body": "strict-lint now lints projects whose ESLint config is any `eslint.config.*` file — `.ts`, `.mts`, `.cts`, `.mjs`, or `.cjs` — not only `eslint.config.js`. TypeScript configs need no build step or separate transformer. Now requires Node 24 or later.",
            "breaking": true,
            "hash": "1890ed83433ad5e61c77790e8e7510d2f4802404"
          }
        ]
      }
    ]
  },
  {
    "version": "7.0.0",
    "date": "2026-07-18",
    "sections": [
      {
        "title": "🎉 Features",
        "audience": "all",
        "items": [
          {
            "description": "Adopt projectService in the published TypeScript config (#86)",
            "body": "The TypeScript preset now resolves each file's owning `tsconfig.json` itself, so type-aware linting no longer needs per-repo parser wiring. Consumers no longer set `parserOptions.project` or maintain a lint-only `tsconfig.eslint.json`; the only setting they keep is `tsconfigRootDir`.\n\nUpgrading is breaking: Remove any `parserOptions.project` and ensure every linted TypeScript file belongs to a discoverable `tsconfig.json`, folding any lint-only `tsconfig.eslint.json` include globs into the real `tsconfig.json` before deleting it.",
            "breaking": true,
            "hash": "5d8cb5f19540feca4425a512189f088ec31c92d8"
          }
        ]
      },
      {
        "title": "⚙️ Tooling",
        "audience": "dev",
        "items": [
          {
            "description": "Migrate the build to nmr-compile by converging packages on the canonical src/ layout (#75)",
            "body": "The two compiled packages now build on the project's shared build tooling, so upgrading that tooling no longer risks silently breaking either package's build. Anyone importing either package's main entry now receives its type declarations — one of the two previously shipped its main entry without them.",
            "hash": "55d9b0dcf21d250fa2b50d5b6fb29c784f5bbb60"
          },
          {
            "description": "Reduce tsconfigs to deviations from TypeScript 6 defaults (#83)",
            "body": "Streamlines TypeScript configs by removing settings that were identical to the default.\n\nTwo behavior changes are included: Unused function parameters now raise a type error (prefix a parameter's name with `_` to exempt one), and the compilation target now matches the supported Node version instead of trailing it.",
            "hash": "feeba72a9affe22cb99d37859843eb25d6202a2f"
          },
          {
            "description": "Simplify nmr config by adopting devBin for strict-lint (#88)",
            "body": "Consolidates the location of the strict-lint source entry point into a single record in the repo's nmr config, replacing three differently spelled copies. `nmr lint:strict` behaves exactly as before in every context — repo root and workspaces — but a future move of the entry point is now a one-line config change instead of a multi-file hunt, and there is no longer a stale copy that could silently break the command.",
            "hash": "bc71879654a8c1f19ef86109e81c3b590631d692"
          }
        ]
      },
      {
        "title": "📦 Dependencies",
        "audience": "dev",
        "items": [
          {
            "description": "Upgrade dependencies to latest and drop Node 18/20 support (#78)",
            "body": "Consuming a published config now requires ESLint 10 and Node `^22.13.0 || >=24`; support for Node 18 and 20 is dropped.",
            "breaking": true,
            "hash": "75ee76c17e9219773e65f608b56ccf6b9443cb74"
          }
        ]
      }
    ]
  },
  {
    "version": "6.3.0",
    "date": "2026-05-01",
    "sections": [
      {
        "title": "⚙️ Tooling",
        "audience": "dev",
        "items": [
          {
            "description": "Adopt the nmr script runner (#58)",
            "body": "Replaces the hand-rolled `run-workspace-script.ts` infrastructure and custom utility scripts with `@williamthorsen/nmr`, a context-aware script runner for pnpm monorepos. The root `package.json` scripts go from ~30 entries to 3 (`ci`, `postinstall`, `prepare`), and the consistency tests are replaced with a single `runConsistencyChecks` import from nmr. In total, ~635 lines are removed and ~76 added.",
            "hash": "c2b5ff03e0bca7562764eb00a64daa9c3b76cb59"
          }
        ]
      }
    ]
  },
  {
    "version": "6.2.1",
    "date": "2026-03-28",
    "sections": [
      {
        "title": "🐛 Bug fixes",
        "audience": "all",
        "items": [
          {
            "description": "Handle numeric severity 1 in convertWarnToError (#50)",
            "body": "`convertWarnToError` now handles ESLint's numeric severity `1` in both bare and array forms. The array-form branch no longer incorrectly targets numeric `2` (which was a no-op converting error to error).",
            "hash": "89322752601b75b926a94d74f62b6fe6863751b2"
          }
        ]
      }
    ]
  },
  {
    "version": "6.2.0",
    "date": "2026-03-23",
    "sections": [
      {
        "title": "🎉 Features",
        "audience": "all",
        "items": [
          {
            "description": "Modify strict-lint to accept ESLint CLI arguments (#49)",
            "body": "Adds full ESLint CLI argument support to `strict-lint`. A new `parseCliArgs` module parses `process.argv` using `node:util` `parseArgs` and maps flags to ESLint constructor options, file patterns, rule overrides, and output-control flags. The programmatic API gains `patterns` and `ruleOverrides` fields for parity.",
            "hash": "951c8b6f1a05a24ee9ff613078684d0fbb743c66"
          }
        ]
      }
    ]
  },
  {
    "version": "6.1.2",
    "date": "2026-03-12",
    "sections": [
      {
        "title": "⚙️ Tooling",
        "audience": "dev",
        "items": [
          {
            "description": "Migrate to release-kit v1.0.0 CLI (#47)",
            "body": "Upgrade `@williamthorsen/release-kit` from v0.2.3 to v1.0.0 and migrate from the script-based release preparation to the CLI-driven approach. The CLI auto-discovers workspaces from `pnpm-workspace.yaml`, replacing the manually maintained component list and custom scripts.",
            "hash": "2a344160060cb1b67716fec0bdf9b65338fdd0d4"
          }
        ]
      }
    ]
  },
  {
    "version": "6.1.1",
    "date": "2026-03-11",
    "sections": [
      {
        "title": "🐛 Bug fixes",
        "audience": "all",
        "items": [
          {
            "description": "Exit 0 when only warnings are reported (#44)",
            "body": "Replaced the regex-based exit logic in `strictLint()` with an `errorCount` check on ESLint's `LintResult[]` objects. `doLint()` now returns `{ text, errorCount }` and `strictLint()` exits 1 only when `errorCount > 0`. Also refactored `strictLint()` from `.then()/.catch()` promise chaining to `async/await` with `try/catch`.",
            "hash": "326b43b19bbd16dc0d2482ed7d6e33cf66820579"
          }
        ]
      }
    ]
  },
  {
    "version": "6.0.0",
    "date": "2026-03-11",
    "sections": [
      {
        "title": "🎉 Features",
        "audience": "all",
        "items": [
          {
            "description": "Add maxSeverity support to exclude rules from escalation (#42)",
            "body": "Add a three-layer `maxSeverity` merge system that controls which rules are excluded from warn-to-error escalation.\n\nBuilt-in defaults ship with `@typescript-eslint/no-deprecated` set to 'warn'.\n\nA project-level `.config/strict-lint.config.ts` file can override defaults, and programmatic options passed to `strictLint()` take highest precedence.\n\nThe `strictLint()` signature changes from positional `baseConfig` to an options object (`StrictLintOptions`).\n\nThe `convertWarnToError` function accepts an optional `MaxSeverityMap` that skips escalation for rules mapped to 'warn'.\n\nA new `loadStrictLintConfig` loader uses tsx's `tsImport` to dynamically load TypeScript config files with shape validation.",
            "breaking": true,
            "hash": "a2484f12c5ffe943857c1a99b44e9c3af32b8065"
          }
        ]
      }
    ]
  }
]
